@using RecruitmentApp.Areas.Admin.Identity.Models.ManageViewModels
@model SetPasswordViewModel

@{
    ViewData["Title"] = "Đặt mật khẩu";
}

<!-- Breadcrumb -->
<nav class="page-breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">Tài khoản</li>
        <li class="breadcrumb-item active" aria-current="page">
            <a asp-action="SetPassword">Đặt mật khẩu</a>
        </li>
    </ol>
</nav>

<!-- Tiêu đề -->
<div class="row">
    <div class="col-md-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <h4 class="font-weight-bold">@ViewData["Title"]</h4>
                <p class="text-muted mt-2">
                    Tài khoản của bạn chưa có mật khẩu, hãy thiết lập để có thể đăng nhập mà không cần dùng tài khoản ngoài.
                </p>
            </div>
        </div>
    </div>
</div>

<!-- Form -->
<div class="row">
    <div class="col-md-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="col-md-6">
                    <form asp-controller="Manage" asp-action="SetPassword" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="form-group">
                            <label asp-for="NewPassword" class="control-label"></label>
                            <input asp-for="NewPassword" class="form-control" />
                            <span asp-validation-for="NewPassword" class="text-danger small"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="ConfirmPassword" class="control-label"></label>
                            <input asp-for="ConfirmPassword" class="form-control" />
                            <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                        </div>

                        <div class="form-group d-flex gap-2 mt-4">
                            <button type="submit" class="btn btn-primary d-inline-flex align-items-center">
                                <i data-feather="lock" class="me-1 icon-md"></i> Đặt mật khẩu
                            </button>
                            <a asp-action="Index" asp-controller="Manage" class="btn btn-outline-secondary d-inline-flex align-items-center">
                                <i data-feather="x" class="me-1 icon-md"></i> Huỷ
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Back link -->
<a asp-action="Profile" asp-controller="Manage" class="btn btn-sm btn-outline-secondary d-inline-flex align-items-center mt-3">
    <i data-feather="chevron-left" class="me-1 icon-md"></i> Quay về trang tài khoản
</a>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
    <script>
        feather.replace();
    </script>
}
